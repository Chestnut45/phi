#pragma once

namespace Phi
{
    // Represents a voxel world and all of its data
    // Provides an interface to load data, generate terrain, and simulate the world
    class VoxelWorld
    {

    };
}